# macif-light-fcmetz

Animation commerciale FC Metz — version HTML autonome (style Marion).

## Déploiement Vercel

Site statique : racine du repo, fichier `index.html`.

## Firebase (optionnel)

Configurer `FIREBASE_CONFIG.databaseURL` dans `index.html` pour la synchro multi-postes.
